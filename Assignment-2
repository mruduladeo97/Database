                                      Assignment - 2

    1) Which field of the Customers table is the primary key?

    Ans: A primary key is a field in a table which uniquely identifies each row/record in a database table.
         Primary keys must contain unique values. A primary key column cannot have NULL values.

         A table can have only one primary key, which may consist of single or multiple fields.
         When multiple fields are used as a primary key, they are called a composite key.

         If a table has a primary key defined on any fields, then you cannot have two records having
         the same value of that field.
          
         

    2) What is the 4th column of the Customers table?
 
    Ans: Customer table 4th column is Rating. 
         so to have only rating column,
        
         select rating from customer;

    3) What is another word for row? For column?

    Ans: another word for row is :    1. Tuple
                                      2. Entity
                                      3. Opportunity
         
         another word for column is : 1. Attribute
                                      2. Method


    4) Why isn’t it possible to see the first five rows of a table?
 
    Ans: When you update a row if the row lenght is incresed the entire roe may move
         to some other values address.later when you select from table you will find row 
         at some other position in the output. hence, it is not possible to view first or
         last row of the table.
